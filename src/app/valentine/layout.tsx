export const metadata = {
  title: "Valentine ✨",
  robots: {
    index: false,
    follow: false,
  },
}

export default function ValentineLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
